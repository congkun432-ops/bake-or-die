local CoreGui = game:GetService("CoreGui")
if CoreGui:FindFirstChild("Maintenance_Notice") then 
    CoreGui["Maintenance_Notice"]:Destroy() 
end
local ScreenGui = Instance.new("ScreenGui", CoreGui)
ScreenGui.Name = "Maintenance_Notice"
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 440, 0, 270) 
MainFrame.Position = UDim2.new(0.5, -220, 0.5, -135)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
MainFrame.BackgroundTransparency = 0.1
MainFrame.Active = true
MainFrame.Draggable = false 

local Corner = Instance.new("UICorner", MainFrame)
Corner.CornerRadius = UDim.new(0, 12)

local Stroke = Instance.new("UIStroke", MainFrame)
Stroke.Color = Color3.fromRGB(255, 140, 0)
Stroke.Thickness = 2
Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

local CloseBtn = Instance.new("TextButton", MainFrame)
CloseBtn.Name = "CloseButton"
CloseBtn.Size = UDim2.new(0, 28, 0, 28)
CloseBtn.Position = UDim2.new(1, -38, 0, 10) 
CloseBtn.BackgroundColor3 = Color3.fromRGB(230, 50, 50) 
CloseBtn.BackgroundTransparency = 0.1
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 14
CloseBtn.ZIndex = 5 

local CloseCorner = Instance.new("UICorner", CloseBtn)
CloseCorner.CornerRadius = UDim.new(0, 6)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

local Icon = Instance.new("ImageLabel", MainFrame)
Icon.Name = "MaintenanceIcon"
Icon.Size = UDim2.new(0, 60, 0, 60)
Icon.Position = UDim2.new(0.5, -30, 0, 15) 
Icon.BackgroundTransparency = 1
Icon.Image = "rbxassetid://134586557209278"
Icon.ScaleType = Enum.ScaleType.Fit

local IconCorner = Instance.new("UICorner", Icon)
IconCorner.CornerRadius = UDim.new(0, 8) 

local MsgLabel = Instance.new("TextLabel", MainFrame)
MsgLabel.Size = UDim2.new(1, 0, 0, 80)
MsgLabel.Position = UDim2.new(0, 0, 0, 85) 
MsgLabel.Text = "Script đang bảo trì! Sẽ sớm hoạt động lại.\n[Script under maintenance! Will be back online soon.]"
MsgLabel.TextColor3 = Color3.fromRGB(245, 245, 245)
MsgLabel.Font = Enum.Font.GothamBold
MsgLabel.TextSize = 16 
MsgLabel.BackgroundTransparency = 1
MsgLabel.LineHeight = 1.3

local TimeLabel = Instance.new("TextLabel", MainFrame)
TimeLabel.Size = UDim2.new(1, 0, 0, 50)
TimeLabel.Position = UDim2.new(0, 0, 0, 185)
TimeLabel.Text = "Đang lấy thời gian hệ thống...\n[Fetching system time...]"
TimeLabel.TextColor3 = Color3.fromRGB(255, 180, 0)
TimeLabel.Font = Enum.Font.GothamMedium
TimeLabel.TextSize = 14
TimeLabel.BackgroundTransparency = 1

local TIME1  = 20
local TIME2 = 0  

task.spawn(function()
    while ScreenGui and ScreenGui.Parent do
        local dateTable = os.date("!*t", os.time() + 7 * 3600)
        local currentSeconds = (dateTable.hour * 3600) + (dateTable.min * 60) + dateTable.sec
        local targetSeconds = (TIME1 * 3600) + (TIME2 * 60)
        local timeRemaining = targetSeconds - currentSeconds
        if timeRemaining < 0 then
            timeRemaining = timeRemaining + 86400
        end
        local hours = math.floor(timeRemaining / 3600)
        local minutes = math.floor((timeRemaining % 3600) / 60)
        local seconds = timeRemaining % 60
        
        TimeLabel.Text = string.format(
            "Thời gian mở: %02d giờ %02d phút %02d giây\n[Opening in: %02d hours %02d minutes %02d seconds]", 
            hours, minutes, seconds, hours, minutes, seconds
        )
        task.wait(1)
    end
end)

task.spawn(function()
    local ran = Random.new()
    while ScreenGui and ScreenGui.Parent do
        task.wait(0.3) 
        
        local RainIcon = Instance.new("ImageLabel", ScreenGui)
        RainIcon.Name = "RainIcon"
        
        local size = ran:NextInteger(20, 40)
        RainIcon.Size = UDim2.new(0, size, 0, size)
        
        RainIcon.Position = UDim2.new(ran:NextNumber(0, 0.95), 0, 0, -50)
        RainIcon.BackgroundTransparency = 1
        RainIcon.Image = "rbxassetid://134586557209278"
        RainIcon.ImageTransparency = ran:NextNumber(0.3, 0.6)
        
        local RainCorner = Instance.new("UICorner", RainIcon)
        RainCorner.CornerRadius = UDim.new(0, 6)
        
        local dropTime = ran:NextNumber(2.5, 4.5) 
        RainIcon:TweenPosition(
            UDim2.new(RainIcon.Position.X.Scale, 0, 1, 10), 
            Enum.EasingDirection.Out,
            Enum.EasingStyle.Linear,
            dropTime,
            true,
            function()
                RainIcon:Destroy() 
            end
        )
    end
end)
